# nixpacks.toml
# Tells Railway to install both Python and Node.js for dual-runtime support.

[phases.setup]
nixPkgs = ["python3", "nodejs"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install --prefix jiosaavn-api"
]
